# Python
__pycache__
*.pyc
*.pyo
*.pyd
.eggs
*.egg-info
dist/
build/

# Virtual environment
.venv
.pytest_cache

# Git
.git
.gitignore

# IDE / Editor
.vscode
.idea
*.swp
*.swo
*.swn

# OS files
.DS_Store
Thumbs.db

# Test files (not needed in production image)
tests/

# Documentation (not needed at runtime)
PRD.md
PLAN.md
DESIGN.md

# Design assets
Design files/

# Local data (created fresh in container)
data/*.db
data/*.db-shm
data/*.db-wal
data/uploads/*
data/backups/*

# Environment files (secrets)
.env
.env.local

# Scripts (not needed at runtime)
scripts/

# FastContext cache
.fastcontext/

# Install scripts
install-fastcontext-laptop.sh

# Source data files (imported into DB, not needed at runtime)
initiatives/
Inventarisatie_AI_initiatieven_v0.1.md

# Alembic (migrations run at build or startup)
alembic/
alembic.ini

# Note: uv.lock is needed for Docker build — do not exclude it
